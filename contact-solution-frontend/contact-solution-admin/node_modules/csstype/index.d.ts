import React from 'react'
import ReactDOM from 'react-dom/client'
import App from './App.tsx'
import './index.css'

// Este arquivo é a ponte entre o seu HTML e o seu código React.
// Ele garante que o componente <App /> seja renderizado dentro da <div id="root"></div>

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
)